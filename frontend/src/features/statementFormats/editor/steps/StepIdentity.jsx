/** Filled in chunk 6. */
export default function StepIdentity({ editor }) {
  return (
    <div className="fe-step">
      <h2>Name</h2>
      <p className="fe-step__sub">How this format appears in account settings and the upload picker.</p>
      <div className="fe-step__placeholder">bank_name / account_type inputs — chunk 6</div>
    </div>
  );
}