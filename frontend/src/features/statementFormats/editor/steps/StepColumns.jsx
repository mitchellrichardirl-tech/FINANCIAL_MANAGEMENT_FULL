/** Filled in chunk 5. */
export default function StepColumns({ editor }) {
  return (
    <div className="fe-step">
      <h2>Columns</h2>
      <p className="fe-step__sub">Tell us which column holds the date, amount and description.</p>
      <div className="fe-step__placeholder">Column mapping form — chunk 5</div>
    </div>
  );
}